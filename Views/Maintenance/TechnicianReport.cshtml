@{
    ViewData["Title"] = "تقرير إنتاجية الفني";
}

<div class="container mt-4">
    <h2 class="text-right">📊 تقرير أداء الفنيين (المصنعية فقط)</h2>
    <hr />

    <div class="card mb-4 border-primary">
        <div class="card-body">
            <form method="get" class="row">
                <div class="col-md-3">
                    <label>الفني</label>
                    <select name="technicianId" class="form-control select2">
                        <option value="">-- الكل --</option>
                        @foreach (var tech in ViewBag.Technicians) {
                            <option value="@tech.Id">@tech.FullName</option>
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <label>الشهر</label>
                    <input type="number" name="month" class="form-control" value="@DateTime.Now.Month" min="1" max="12" />
                </div>
                <div class="col-md-2">
                    <label>السنة</label>
                    <input type="number" name="year" class="form-control" value="@DateTime.Now.Year" />
                </div>
                <div class="col-md-2">
                    <label>نسبة الفني (%)</label>
                    <input type="number" name="commissionRate" class="form-control" placeholder="مثلاً: 10" step="0.1" />
                </div>
                <div class="col-md-3">
                    <label>&nbsp;</label>
                    <button type="submit" class="btn btn-primary btn-block">عرض التقرير 🔍</button>
                </div>
            </form>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card bg-info text-white text-center p-3">
                <h5>عدد العمليات</h5>
                <h2 class="font-weight-bold">@ViewBag.TotalOperations</h2>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-success text-white text-center p-3">
                <h5>إجمالي دخل الصيانة (يد)</h5>
                <h2 class="font-weight-bold">@ViewBag.TotalServiceRevenue.ToString("N2")</h2>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-dark text-white text-center p-3">
                <h5>صافي ربح الفني (@ViewBag.CommissionRate %)</h5>
                <h2 class="font-weight-bold text-warning">@ViewBag.TechnicianProfit.ToString("N2")</h2>
            </div>
        </div>
    </div>
</div>